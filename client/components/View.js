var React = require('react');
var ReactDOM = require('react-dom');

const View = module.exports = (props) => {

  return <div className="container">
    <div>
      <p>Stuff gon go here</p>
    </div>
  </div>

}
